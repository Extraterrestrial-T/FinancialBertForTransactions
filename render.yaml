services:
  - type: web
    name: pragma-lite-explorer
    runtime: python
    plan: free
    buildCommand: pip install -e ".[web]"
  startCommand: gunicorn --workers 1 --threads 1 --timeout 120 --bind 0.0.0.0:$PORT webapp.server:app
    healthCheckPath: /api/bootstrap
